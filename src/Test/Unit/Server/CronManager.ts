import { CronManager } from '../../../Server/CronManager';

describe("Server/CronManager", function () {
    it("test command", function () {
        let tested: boolean = false;
        let commander = new CronManager();
    });
});