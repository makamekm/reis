pipeline:
  # init:
  #   image: node:9.3.0
  #   commands:
  #     - npm i

  # build:
  #   image: node:9.3.0
  #   commands:
  #     - rm -rf build
  #     - npm run build

  publish:
    image: node:9.3.0
    secrets: [ NPM_USERNAME, NPM_PASSWORD, NPM_EMAIL ]
    commands:
      - node publish.js
    when:
      branch: [master]