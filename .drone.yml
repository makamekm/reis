pipeline:
  # init:
  #   image: node:9.3.0
  #   commands:
  #     - npm i

  # build:
  #   image: node:9.3.0
  #   commands:
  #     - rm -rf build
  #     - npm run build

  publish:
    image: node:9.3.0
    secrets: [ npm_username, npm_password, npm_email ]
    commands:
      - node publish.js ${npm_username} ${npm_password} ${npm_email}
      - env
    when:
      branch: [master]